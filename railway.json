{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "rm -rf .next/cache/turbopack && npm run build"
  },
  "deploy": {
    "startCommand": "node scripts/migrate.mjs && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
